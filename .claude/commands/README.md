
# Claude-Code Commands (Final Full Version)

Commands included:
- /plan (always creates exactly 10 phases)
- /program (auto feature-flag control)
- /execute (with added sanity-checks)
- /migrate (convert legacy plans into new 10-phase system)

Use order:
1. /plan
2. /program
3. /execute (for manual phase execution)
4. /migrate (legacy → new)
