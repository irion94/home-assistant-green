/**
 * Design System
 *
 * Re-exports all design system tokens and utilities.
 *
 * Usage:
 * import { colors, spacing, getVoiceStateColor } from '@/design-system'
 */

export {
  colors,
  spacing,
  typography,
  borderRadius,
  animation,
  breakpoints,
  zIndex,
  getVoiceStateColor,
  getEntityStateColor,
  type VoiceState,
  type EntityState,
  type IconSize,
} from './tokens'
