##############################
# Home Assistant – SAFE GITIGNORE
##############################

# --- CRITICAL: NEVER COMMIT THESE ---
# HA internal registry + credentials
.storage/
secrets.yaml

# HA database (history, energy etc.)
home-assistant_v2.db
home-assistant_v2.db-*
*.db
*.db-shm
*.db-wal

# Backups / snapshots
backups/
*.tar
*.zip

# HA runtime directories
deps/
tts/
.cloud/

# Logs
*.log
*.log.*
config/home-assistant.log

# Temporary files
*.tmp
*.swp
*.swo
*.orig

# Python cache (for custom_components)
__pycache__/
*.pyc
*.pyo

# VSCode
.vscode/
.code/
.history/

# HA Add-ons local files
addons/local/*

# HACS (safe to version config but not internal storage)
www/community/*
config/custom_components/hacs/

# Node / JS temp (if used anywhere)
node_modules/

# Pipenv / virtual envs
venv/
ENV/
.env
.env.*

# System-level rubbish
.DS_Store
Thumbs.db

##############################
# EXTRA SAFETY FOR ENTERPRISE MODE
##############################

# Prevent committing local device config
.ipython/
.python_history

# Temporary deployment files
deploy-artifacts/
tmp/
cache/

# CI files (if generated locally)
coverage/
pytest_cache/
.mypy_cache/
.tox/

##############################
# INVENTORY DATA
##############################

# Raw registry snapshots (can contain sensitive data, large files)
data/inventory/raw/

# Ephemeral HA runtime/version files
config/.HA_VERSION
config/.ha_run.lock
